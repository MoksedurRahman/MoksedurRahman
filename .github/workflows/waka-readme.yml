name: Waka Readme

on:
  # Manual trigger
  workflow_dispatch:

  # Scheduled daily at 12:00 AM UTC
  schedule:
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: WakaReadme DevMetrics
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}

          # ✅ Add these to enable output
          SHOW_TITLE: true
          BLOCKS: -> # 
          TIME_RANGE: all_time 
          LANG_COUNT: 10
          SHOW_TIME: true
          SHOW_TOTAL: true
          CODE_LANG: true
